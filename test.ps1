# goes to your steam guard grabber file's location (if the location is outside of the folder then you will have to insert your path).
Set-Location steam-guard-grabber-main
#executes index.js to write the new steam auth code into the text file, you also have to INSTALL NODEJS FOR THIS
node.exe index.js
